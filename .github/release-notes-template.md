## Release checklist
- [x] Build executed with deterministic mode.
- [x] `dist/manifest.json` updated.
- [x] SHA256 checksums generated under `dist/checksums/`.

## Notes
- Legacy and modern exports are attached from `dist/*`.
- `.srs` compilation remains optional and is intentionally not required in CI.
